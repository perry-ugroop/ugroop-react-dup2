/**
 * Created by Ber on 25/11/16.
 */
import BSContainer from '../../containers/BootStrap/BSContainer';
import styled from 'styled-components';

const UGContainer = styled(BSContainer)`
  &&{
    position:relative;
  }
`;

export default UGContainer;

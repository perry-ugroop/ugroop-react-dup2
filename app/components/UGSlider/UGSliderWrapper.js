
import styled from 'styled-components';
import BSContainer from '../../containers/BootStrap/BSContainer';

const UGSliderWrapper = styled(BSContainer)`
  position:relative;
`;

export default UGSliderWrapper;

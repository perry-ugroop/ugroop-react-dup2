import styled from 'styled-components';

const UGSliderImageStyle = styled.div`
  &&{ 
  text-align: right;
  height:150px;
  }
  && img.ugsliderimg{
    position:absolute;
    top:10px;
    right:20px;
  }
`;

export default UGSliderImageStyle;
